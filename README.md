# Daytona

Static site, no build. Open `index.html`.

- `games/flappy/` — Flappy Bird clone (canvas, synthesized sound).
- `games/connections/` — Connections clone. Puzzles live in `puzzles.js`; the daily puzzle is picked by date, `?p=N` opens a specific one.
